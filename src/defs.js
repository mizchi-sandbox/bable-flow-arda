/* @flow */

interface Props {
  n: number;
}

interface State {
  foo: string;
}

interface ComponentProps {
  foo: string;
}

module.exports = {
  Props: Props, State: State, ComponentProps: ComponentProps
}
